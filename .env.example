# Root folder that the bridge may read.
BRIDGE_ROOT=C:\path\to\project

# Generate a long random value. Never commit the real token.
BRIDGE_TOKEN=change-me

# Maximum bytes returned by one read-range call.
BRIDGE_MAX_READ_BYTES=4194304

# Local audit log path.
BRIDGE_AUDIT_LOG=bridge-audit.log
